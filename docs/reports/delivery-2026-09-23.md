# 2026-09-23 交付与新增功能实测

环境：`star-chart:cpu`，Python 3.11 slim-bookworm，`requirements.txt` 固定的科学计算栈，
通过 Debian 安装 Noto CJK 字体。`docker image ls` 大小 1.01 GB（旧 interim 为 21 GB）。

## 验证

- 原有测试与打包测试共 573 条：567 passed、5 skipped、1 xfailed，0 failures/errors。
  JUnit：`output/validation/cpu-validation.xml`，耗时 892.852 秒。
- 新增恒星候选、传递导入隔离及打包测试：14 passed，JUnit：`output/validation/delivery-tests.xml`。
  其中 4 条打包检查与前项重复，不叠加当作新增测试数。
- 新 CLI 在数据集 B 上运行成功，耗时约 144 秒。生成完整报告、两份 CSV 和标注图。
- 空场景导出、结果覆盖保护另有 2 条测试通过，JUnit：`output/validation/cli-tests.xml`。

## 数据集 B，指定第 30 帧

命令见 `docs/delivery.md`。核验窗口 f27–f32，最少 4/6 帧匹配，半径 3 px，
5σ 探测，固定测光孔径半径 5 px。边界截断孔径和非正流量不进入最暗候选筛选。

| 指标 | 实测 |
|---|---|
| 指定图像文件 | `20260721172659164_6002_stand.fits` |
| 去坏点探测源数 | 93 |
| 跨帧确认恒星候选数 | 71 |
| 最暗候选位置（0-based x,y） | (326.810026, 3867.780032) |
| 最暗候选命中数 | 5/6 |
| 指定帧孔径流量 | 400.708471 ADU |
| 曝光归一化仪器星等 | −10.314268 |
| 真值定标星等估计 | 9.142776（对外建议写约 9.14） |
| ADU/s 零点 | 19.457045 |
| 零点拟合点数与散度 | 54 点，0.118268 mag；这是拟合散度，不是总误差 |
| 目标数 / 轨迹位置行数 | 1 / 54，f17–f70 |
| 探测器系 / 天球系速度 | 1.843980 / 126.095851 px/帧 |
| 静止性对比度 | 68.382437 |

目标判决与历史实测一致。93 源是去坏点后的结果，历史 95 源包含本次已剔除的缺陷。
71 个候选是参考 f30、核验 f27–f32 的结果，不与历史参考 f17 的复现率 0.6304 混写。

**最暗候选星等 9.14 与历史第 95 百分位 10.2111 不可直接比较。** 前者先做跨帧筛选，
且使用固定孔径 ADU/s 定标；后者对全部单帧分割流量取百分位。候选集合、测光定义、
零点三者都不同。本次未做完备性校验，也未对未知滤光波段作标准星表校正。

## 数据集 A，无真值路径

`--input data/images/60394_20260309_1437117414637934_PIC --reference-frame 16 --no-plot`
运行通过，结果在 `output/contest-A/`。未启用真值定标。

| 指标 | 实测 |
|---|---|
| 目标候选数 / 轨迹行数 | 1 / 31，f5–f35 |
| 探测器系 / 天球系速度 | 3.271615 / 181.395629 px/帧 |
| 静止性对比度 | 55.445294 |
| 第 16 帧去坏点探测数 / 跨帧确认候选数 | 96 / 76 |
| 最暗候选位置（0-based x,y） | (1180.0, 3298.401961) |
| 最暗候选命中数 | 4/6 |
| 定标星等 | `null`（无零点依据） |

这是双系判据给出的目标候选；由于 A 没有真值，不给出与真值吻合、目标身份或标准
星等的结论。A 与 B 各检出一条，也不等于在同一个序列中检出多个真实目标。
