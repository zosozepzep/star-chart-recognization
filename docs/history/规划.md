# 天文星点检测项目技术优化策略（工程化路线图）

> 目标：将当前以启发式规则为主的检测系统，升级为一个**物理约束驱动、可量化评估、可扩展维护**的工业级天文目标检测流水线。

---

# 1. 项目总体目标

核心问题：

> **在强噪声、海量恒星背景和复杂伪目标条件下，稳定识别真实天体，并输出可信的物理参数。**

因此系统目标包括：

1. 高召回率（尽量不漏检）
    
2. 低误检率（抑制噪声和坏点）
    
3. 亚像素定位精度
    
4. 多帧时序一致性
    
5. 物理可解释性
    
6. 工程可维护性
    
7. 可与标准工具（SExtractor）定量对比
    

---

# 2. 核心设计思想

## 2.1 当前架构的问题

当前 pipeline 的问题在于：

- 检测模块复杂，但判决标准不统一
    
- 各引擎结果缺乏统一评分
    
- 去重逻辑偏启发式
    
- 多帧信息利用不足
    
- 缺乏系统级 benchmark
    

## 2.2 设计理念

将整个系统重构为：

```text
Raw FITS
  ↓
Background Modeling
  ↓
Candidate Generation (High Recall)
  ↓
Feature Extraction
  ↓
Confidence Scoring
  ↓
Spatial NMS
  ↓
Temporal Association
  ↓
Physical Validation
  ↓
Final Catalog
```

核心思想：

> **候选生成负责高召回，评分系统负责判别，多帧关联负责最终裁决。**

---

# 3. 系统架构规划

## 3.1 目录结构

```text
src/
├── main_pipeline.py
├── detector/
│   ├── candidate_generator.py
│   ├── feature_extractor.py
│   ├── psf_fitter.py
│   ├── scorer.py
│   └── nms.py
├── tracking/
│   ├── associator.py
│   ├── kalman.py
│   └── physical_validator.py
├── benchmark/
│   ├── sextractor_runner.py
│   ├── evaluator.py
│   └── metrics.py
├── visualization/
│   ├── preview.py
│   ├── plots.py
│   └── report.py
└── utils/
```

---

# 4. Phase 1：单帧检测体系优化

---

## 4.1 Background Modeling（背景建模）

### 目标

准确估计：

- 背景图 `B(x,y)`
    
- RMS 图 `σ(x,y)`
    

### 实现

使用 `Background2D + SigmaClip`。

### 输出

- `background.fits`
    
- `rms.fits`
    

### 工程化优化

所有阈值都基于局部 RMS，而非全局固定阈值。

---

## 4.2 Candidate Generation（候选生成）

目标：**宁可多报，不可漏报。**

### 引擎 A：DAOStarFinder

适合点状目标。

### 引擎 B：Segmentation

适合拖尾或非标准形状。

### 引擎 C：LoG / DoG

用于增强极暗目标。

### 引擎 D：SExtractor 基准

用于 benchmark。

### 输出格式

```json
{
  "x": 123.45,
  "y": 456.78,
  "engine": "DAO",
  "threshold_sigma": 2.0
}
```

---

## 4.3 Feature Extraction（特征提取）

每个候选统一提取以下特征。

### 光度特征

- peak
    
- flux_aperture
    
- flux_auto
    
- local_snr
    
- background
    

### 几何特征

- fwhm_x
    
- fwhm_y
    
- ellipticity
    
- elongation
    
- theta
    

### PSF 拟合特征

- sigma_x
    
- sigma_y
    
- chi_square
    
- residual_ratio
    

### 上下文特征

- distance_to_edge
    
- engine_votes
    
- overlap_count
    

---

## 4.4 PSF Fitting（二维高斯拟合）

### 核心作用

这是单帧精度提升最大的模块。

### 拟合模型

二维高斯 + 常数背景。

### 输出

- 亚像素中心
    
- FWHM
    
- 对称性
    
- 残差
    

### 判据

- `sigma_x / sigma_y` 接近 1
    
- `residual_ratio` 足够小
    

---

## 4.5 Confidence Scoring（统一评分）

### 目标

将所有候选映射到 [0, 1]。

### 推荐公式

```text
score =
  0.30 * snr_score +
  0.25 * psf_score +
  0.15 * shape_score +
  0.15 * vote_score +
  0.10 * edge_score +
  0.05 * flag_score
```

### 说明

评分模块是整个系统的“裁判”。

---

## 4.6 Spatial NMS（空间非极大值抑制）

### 原则

在半径 `r = 2~4 px` 内，仅保留 score 最高的目标。

### 优点

- 去除重复检测
    
- 保留最可信候选
    

---

# 5. Phase 2：多帧关联与轨迹构建

---

## 5.1 Frame-to-Frame Association

使用：

- KDTree
    
- ~~匈牙利算法~~ #  O($N^3$)
    
- Kalman Filter（可选）
    

### 匹配代价

综合：

- 位置距离
    
- 亮度变化
    
- PSF 相似度
    

---

## 5.2 Track Building

每条轨迹记录：

- 连续帧数
    
- 平均速度
    
- 速度方差
    
- 亮度曲线
    
- 线性拟合残差
    

---

## 5.3 Track Scoring

轨迹得分应包括：

- persistence
    
- linearity
    
- velocity consistency
    
- photometric consistency
    

---

# 6. Phase 3：物理审查系统

---

## 6.1 Linearity Test

真实目标在短时间内应近似匀速直线。

### 指标

- R²
    
- RMS residual
    

---

## 6.2 Velocity Consistency

逐帧速度变化不应剧烈。

---

## 6.3 Photometric Stability

亮度变化应符合合理范围。

---

## 6.4 PSF Consistency

同一目标在各帧的 FWHM 应稳定。

---

# 7. 最终置信度模型

建议分层：

```text
single_frame_score
      ↓
track_score
      ↓
physical_score
      ↓
final_score
```

最终仅保留：

- `final_score > threshold`
    
- `track_length >= N_min`
    

---

# 8. Benchmark 与量化评估体系

---

## 8.1 与 SExtractor 对比

使用SExtractor作为标准 baseline。

比较指标：

- 检测数量
    
- 匹配率
    
- 定位误差
    
- 漏检率
    
- 误检率
    
- 运行时间
    

---

## 8.2 单帧指标

- Precision
    
- Recall
    
- F1
    
- Localization RMSE
    

---

## 8.3 多帧指标

- Track completeness
    
- Fragmentation
    
- False tracks
    

---

# 9. 数据管理与中间产物

每个阶段都应落盘。

## 推荐输出

```text
output/
├── background/
├── candidates/
├── features/
├── scored/
├── tracks/
├── reports/
└── final_catalog/
```

这样便于回溯与调试。

---

# 10. 可视化体系

## 单帧

- 原图 + 检测框
    
- SNR 热图
    
- score 分布
    

## 多帧

- 轨迹图
    
- 亮度曲线
    
- 速度曲线
    

## Benchmark

- Precision-Recall 曲线
    
- ROC 曲线
    
- 对比柱状图
    

---

# 11. 参数管理

所有参数放入 YAML。

```yaml
background:
  box_size: 128

detection:
  threshold_sigma: 2.0

psf:
  patch_size: 11

tracking:
  max_distance: 5
```

---

# 12. 自动化实验框架

支持：

- 参数 sweep
    
- 批量 benchmark
    
- 自动生成报告
    

例如：

```bash
python run_experiments.py --config configs/*.yaml
```

---

# 13. 性能优化

## CPU

- NumPy 向量化
    
- ~~numba~~ 环境配置过于复杂，日后考虑
    
- 多进程
    

## GPU（可选）

- CuPy
    
- PyTorch
    

### 目标

优先保证算法正确性，再进行加速。

---

# 14. 软件工程

## 日志

使用结构化 logging。

## 类型注解

全面使用 type hints。

## 单元测试

- PSF 拟合
    
- NMS
    
- Scoring
    

## CI

自动运行测试。

---

# 15. 开发优先级

## P0：必须完成

1. Feature Extraction
    
2. PSF Fitting
    
3. Unified Scoring
    
4. Score-based NMS
    
5. Benchmark with SExtractor
    

## P1：高价值

6. Temporal Association
    
7. Track Scoring
    
8. Physical Validation
    

## P2：锦上添花

9. Kalman Filter
    
10. Hyperparameter Search
    
11. GPU Acceleration
    

---

# 16. 计划

## Week 1

- 完成 feature extractor
    
- 完成 PSF fitter
    

## Week 2

- 实现 scorer + NMS
    
- 完成 benchmark
    

## Week 3

- 多帧关联
    
- 轨迹评分
    

## Week 4

- 自动化实验
    
- 参数优化
    

---

# 17. 设计原则

### Principle 1

候选生成负责高召回。

### Principle 2

评分系统负责最终裁决。

### Principle 3

多帧信息比单帧信息更可信。

### Principle 4

物理约束优先于经验规则。

### Principle 5

所有模块都要可量化评估。

---

# 18. 技术突破点

> **实现二维高斯 PSF 拟合 + 统一评分系统。**

> **加入多帧轨迹一致性评分。**

这两步将显著提高精度。

---

# 19. 目标系统的最终能力

最终系统应具备：

- 单帧高召回检测
    
- 亚像素定位
    
- 多帧稳健跟踪
    
- 物理一致性验证
    
- 自动 benchmark
    
- 自动报告生成
    

---

# 20. 技术路线总结

整个优化路线可概括为：

> **从“多规则启发式检测器”，升级为“物理约束驱动的分层概率决策系统”。**

其本质是：

1. 高召回候选生成
    
2. 多维特征提取
    
3. 统一置信度评分
    
4. 多帧轨迹关联
    
5. 物理一致性审查
    
6. Benchmark 驱动迭代
# 附录
你抓住了算法比对中最核心的“盲盒”！

你刚才的直觉极其准确：**那 3000 颗星的差异，有极大概率是因为 SExtractor (简称 SE) 内部某些极其强悍的“保守防御配置”被触发了。**

当我们用自己写的算法去硬刚行业标杆时，如果数量对不上，通常不是谁“写错了”，而是**双方的“宽严尺度（Configuration）”没有对齐**。

让我们直接翻开 SExtractor 的底层配置表（`.sex` 文件），来看看它究竟是用哪四大参数把那 3000 颗星给“抹杀”掉的，以及对应我们流水线里的哪个环节：

### ⚙️ 核心嫌疑 1：面积过滤门槛 (`DETECT_MINAREA`)

- **SExtractor 的逻辑**：默认情况下，SE 会设置 `DETECT_MINAREA = 5`。这意味着，即使某个像素的信噪比高达 100，但如果它周围的高亮像素加起来不到 5 个（面积不够），SE 会铁面无私地判定它是**宇宙射线 (Cosmic Ray)** 或 **热像素 (Hot Pixel)**，直接删除。
    
- **我们的系统**：我们在 `SEG` 引擎里确实设置了 `npixels=5`，但是在 `DAO` 引擎里，由于依赖二维高斯核卷积，极个别强烈的单像素尖峰可能会在卷积后被放大，从而逃过了审查。
    

### ⚙️ 核心嫌疑 2：解混淆策略 (`DEBLEND_NTHRESH` & `DEBLEND_MINCONT`)

- **SExtractor 的逻辑**：这是 SE 最引以为傲的拆星算法。`DEBLEND_MINCONT` (最小对比度) 决定了它多愿意去把粘在一起的星“切开”。如果这个值设得较高（比如 `0.005` 以上），SE 在遇到银河系中心那种密密麻麻的星带时，会极其保守，把 3 颗粘连的星当成 1 颗大胖星报出来。
    
- **我们的系统**：我们的多进程亚像素 PSF 拟合极其暴力（`maxfev=200` 的 Levenberg-Marquardt 迭代）。只要有微小的双峰特征，我们就会生撕硬拽地把它们拟合成两颗星。在这个环节上，往往是**我们找得更全，SE 漏得更多**。
    

### ⚙️ 核心嫌疑 3：背景网格的“吞噬效应” (`BACK_SIZE`)

- **SExtractor 的逻辑**：SE 会用一个网格（比如 `BACK_SIZE = 64`）来评估背景。如果这张异常图里有**大面积的星云**或**杂散光**，如果 `BACK_SIZE` 设得很小（比如 32），SE 会把这片星云本身当成“背景”直接减掉，导致星云内部包裹的上千颗暗星集体失踪。
    
- **我们的系统**：我们的 `HybridBackgroundEstimator` 也用了 `box_size=64`，并进行了极端的 sigma_clip。如果这张图极其特殊，我们的背景地平线可能比 SE 压得更低，导致暗星全部浮出了水面。
    

### ⚙️ 核心嫌疑 4：光学伪影清理 (`CLEAN` & `CLEAN_PARAM`)

- **SExtractor 的逻辑**：这是一种非常复杂的“去伪存真”机制。极亮星周围会有十字衍射星芒（Diffraction Spikes）或 CCD 溢出拖尾（Bleeding Trails）。这些拖尾在图像上是一个个的亮斑。如果不加清理，会被误认为几百颗星星。SE 的 `CLEAN = Y` 会利用主星的光度剖面，把周围的这些假目标全部吃掉。
    
- **我们的系统**：我们目前**没有任何针对亮星衍射星芒的专门过滤机制**。如果那张异常图里恰好有几颗极其刺眼的亮星，我们的系统很可能会沿着它的十字星芒，报出几百个假目标。
    

---

### 🔬 终极判决法：不要猜，去“验尸”！

既然我们现在已经具备了生成标准 CSV 星表的能力，我强烈建议你做一个高级算法工程师必做的动作——**交叉匹配 (Cross-Matching)**。

你可以写一个极其简单的脚本，用 `scipy.spatial.cKDTree` 把你的 9000 颗星和 SExtractor 的 6000 颗星进行坐标比对（允许 1~2 像素误差）。

**抓出那多出来的 3000 颗“孤儿星”，然后把它们的坐标在原图（FITS）上用红圈画出来！**

- **如果红圈密密麻麻地围绕在一颗大亮星的十字星芒上** ➡️ 破案了，是我们的系统缺乏 `CLEAN` 机制，我们报了假星。
    
- **如果红圈出现在银河系极其密集的星团中心** ➡️ 破案了，是 SE 的 `DEBLEND` 参数太保守，我们的 PSF 拟合立了大功。
    
- **如果红圈散布在暗弱的星云背景里** ➡️ 破案了，是双方的背景估计 `BACK_SIZE` 不一致。
    

通过把刚才代码里加进去的 **SNR 绝对信噪比**，结合接下来对这 3000 颗异常星的**可视化抽查**，你将彻底掌控这套系统的每一个毛孔。需要我为你快速写一个交叉匹配并画红圈的诊断脚本吗？