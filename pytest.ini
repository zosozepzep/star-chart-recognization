[pytest]
testpaths = tests
norecursedirs = legacy archive dist output data docs .git _probe probe .superpowers
addopts = -q
filterwarnings =
    ignore::astropy.io.fits.verify.VerifyWarning
    ignore:File may have been truncated:astropy.utils.exceptions.AstropyUserWarning
